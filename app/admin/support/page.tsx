// Admin Support Tickets page
import SupportTickets from '@/components/admin/SupportTickets';

export const metadata = {
  title: 'Support — SyncLiving Admin',
};

export default function SupportPage() {
  return <SupportTickets />;
}
